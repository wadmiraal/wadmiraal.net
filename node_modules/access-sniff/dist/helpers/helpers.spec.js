'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = undefined;

var _generateReportLevels = require('./generateReportLevels');

var _generateReportLevels2 = _interopRequireDefault(_generateReportLevels);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Tests = {
  setUp: function setUp(done) {
    done();
  },
  ReportLevels: function ReportLevels(test) {
    var levels = {
      error: true,
      warning: true,
      notice: true
    };

    var reportLevels = void 0;

    reportLevels = (0, _generateReportLevels2.default)(levels);
    test.deepEqual(reportLevels, ['ERROR', 'WARNING', 'NOTICE'], 'Should have all report levels');

    levels = {
      error: true,
      warning: false,
      notice: false
    };
    reportLevels = (0, _generateReportLevels2.default)(levels);
    test.deepEqual(reportLevels, ['ERROR'], 'Should have the error report level');

    test.expect(2);
    test.done();
  }
};

exports.default = Tests;