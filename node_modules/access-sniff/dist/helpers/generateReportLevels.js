'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = undefined;

var _underscore = require('underscore');

var _underscore2 = _interopRequireDefault(_underscore);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var generateReportLevels = function generateReportLevels(reportLevels) {
  var reportLevelsArray = [];

  // We need to convert the report levels to uppercase
  _underscore2.default.each(reportLevels, function (value, key) {
    if (value) {
      reportLevelsArray.push(key.toUpperCase());
    }
  });

  return reportLevelsArray;
};

exports.default = generateReportLevels;